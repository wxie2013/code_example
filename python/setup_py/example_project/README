
->pip install -e .
the package will be installed at: 
    "anaconda3/envs/brian2.5/lib/python3.8/site-packages"

-->pip install -e . --user
the package will be installed at: 
    "/home/wxie/.local/lib/python3.8/site-packages"

--> pip uninstall example2022   # uninstall the package

to use it: 
>>> from example_test_project.example import *
>>> test_setup()
